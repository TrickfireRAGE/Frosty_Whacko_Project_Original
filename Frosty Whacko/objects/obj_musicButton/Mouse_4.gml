/// @description 

event_inherited();

audio_play_sound(snd_soundeffectNotUnlocked, 0, 0); // Only here to give a signal that this isn't available.

//room_goto(rm_shopSounds);

// ^ Commented out due to not having time.